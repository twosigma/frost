#!/bin/sh
# FROST has no hardware entropy source, so the stock BusyBox seedrng script
# would block in getrandom() until the kernel's CRNG is initialized, which can
# take much of the boot. Seeding is skipped; nothing in the boot payload needs
# entropy.
exit 0
