# Boot images from a standalone frost_boot_image.py run in this directory (it
# writes to --out, FROST_OUTDIR, or the current directory). Buildroot writes
# them to $BINARIES_DIR instead.
/frost-nommu-fpga.dts
/frost-nommu-fpga.dtb
/sw.mem
/sw.txt
/sw_ddr.mem
/sw_ddr.txt
