# Register file (Writeback stage) file list
# Generic register file used for both integer and FP register files
# Dependencies: sdp_dist_ram (from lib/ram)

# Generic register file - parameterized by width, read ports, and hardwired zero
$(ROOT)/hw/rtl/cpu_and_mem/cpu/wb_stage/generic_regfile.sv
