# The image installs its core tools from pinned upstream releases; source and
# build products reach the container only through the /workspace mount at run
# time. The image copies in Dockerfile and docker_entrypoint.py, which
# `frost.py doctor` compares with the checkout.
**
!Dockerfile
!docker_entrypoint.py
